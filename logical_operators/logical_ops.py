# logical operators AND  are used to combine two statements together
has_high_income = False
has_good_credit = True
if has_high_income and has_good_credit:
  print('eligible for loan')
else:
  print('not eligible for loan')

  # logical operators or
  # for AND: BOTH condition sshould true
  # for OR : at least one should be true used to combine two statements together
  # NOT